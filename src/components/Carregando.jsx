// src/components/Carregando.jsx — tela simples de carregamento
export const Carregando = ({ texto }) => (
  <div style={{ height: "100vh", display: "grid", placeItems: "center", background: "#0a1118", color: "#7e96ad", fontFamily: "'Segoe UI',sans-serif" }}>{texto}</div>
);
